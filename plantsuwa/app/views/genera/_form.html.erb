<%= simple_form_for(@genus, html: { class: 'form-horizontal' }) do |f| %>
  <% if @genus.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@genus.errors.count, "error") %> prohibited this genus from being saved:</h2>
    <ul>
    <% @genus.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
  	<% end %>
    </ul>
  </div>
  <% end %>
  
  <fieldset>
	  <%= f.input  :name, hint: 'Aim to capture all possible types in about 5-10 different categories.' %>
  </fieldset> 
 <br>
  <p>
    <%= f.submit %>
  </p>
<% end %>