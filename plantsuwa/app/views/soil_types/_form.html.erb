<%= simple_form_for(@soil_type, html: { class: 'form-horizontal' }) do |f| %>
  <% if @soil_type.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@soil_type.errors.count, "error") %> prohibited this soil_type from being saved:</h2>
    <ul>
    <% @soil_type.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
  	<% end %>
    </ul>
  </div>
  <% end %>
  
  <fieldset>
	  <%= f.input  :name, hint: 'Aim to capture all possible types in about 5-10 different categories.' %>
  </fieldset> 
 <br>
  <p>
    <%= f.submit %>
  </p>
<% end %>