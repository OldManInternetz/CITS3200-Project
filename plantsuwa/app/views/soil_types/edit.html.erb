<% provide(:title, 'Edit Soil Type') %>
<% provide(:prev_page, 'Soil Types') %>
<% provide(:prev_link, soil_types_path) %>

<h1 class="h1-shrink"><span class="preceding-heading"><%= link_to 'Soil Types', soil_types_path %> <i class="fa fa-chevron-right"></i></span> Edit '<%= @soil_type.name %>'</h1>
<hr/>

<strong>Note that these changes will be applied to all plants with this soil type.</strong>

<%= render 'form' %>