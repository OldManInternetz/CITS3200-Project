<% provide(:title, 'Edit Size') %>
<% provide(:prev_page, 'Sizes') %>
<% provide(:prev_link, sizes_path) %>

<h1><span class="preceding-heading"><%= link_to 'Sizes', sizes_path %> <i class="fa fa-chevron-right"></i></span> Edit '<%= @size.name %>'</h1>
<hr/>

<%= render partial: 'admin_pages/generic_admin_form', locals: {object: @size, object_name: 'size'} %>