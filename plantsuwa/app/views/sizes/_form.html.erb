<%= simple_form_for(@size, html: { class: 'form-horizontal' }) do |f| %>
  <% if @size.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@size.errors.count, "error") %> prohibited this size from being saved:</h2>
    <ul>
    <% @size.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
  	<% end %>
    </ul>
  </div>
  <% end %>
  
  <fieldset>
	  <%= f.input  :name, hint: 'Aim to capture all possible types in about 5-10 different categories.' %>
  </fieldset> 
 <br>
  <p>
    <%= f.submit %>
  </p>
<% end %>