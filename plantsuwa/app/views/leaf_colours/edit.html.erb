<% provide(:title, 'Edit Leaf Colour') %>
<% provide(:prev_page, 'Leaf Colour') %>
<% provide(:prev_link, leaf_colours_path) %>

<h1 class="h1-shrink"><span class="preceding-heading"><%= link_to 'Leaf Colours', leaf_colours_path %> <i class="fa fa-chevron-right"></i></span> Edit '<div class="colour-circle circle-large" style="background: <%= (@leaf_colour.name).gsub(/\s+/, '') %>"></div> <%= @leaf_colour.name %>'</h1>
<hr/>

<strong>Note that these changes will be applied to all plants with this leaf colour.</strong>

<%= render 'form' %>
