<% provide(:title, @leaf_colour.name) %>

<%= render partial: 'admin_pages/generic_admin_show', locals: {object: @leaf_colour, objects_path: leaf_colours_path, edit_object_path: edit_leaf_colour_path(@leaf_colour), object_path: leaf_colour_path(@leaf_colour), object_prevpage: 'L. Colours', object_name: 'leaf colour', object_has_colour: true } %>

<p>Plants that are '<%= @leaf_colour.name %>':</p>
<%= render 'admin_pages/plants_table' %>