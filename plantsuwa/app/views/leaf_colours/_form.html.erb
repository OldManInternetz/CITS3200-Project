<%= simple_form_for(@leaf_colour, html: { class: 'form-horizontal' }) do |f| %>
  <% if @leaf_colour.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@leaf_colour.errors.count, "error") %> prohibited this leaf colour from being saved:</h2>
    <ul>
    <% @leaf_colour.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
  	<% end %>
    </ul>
  </div>
  <% end %>
  
  <fieldset>
	  <%= f.input  :name, hint: 'Aim to capture all possible types in about 5-10 different categories.' %>
  </fieldset> 
 <br>
  <p>
    <%= f.submit %>
  </p>
<% end %>