<%= simple_form_for(@origin, html: { class: 'form-horizontal' }) do |f| %>
  <% if @origin.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@origin.errors.count, "error") %> prohibited this origin from being saved:</h2>
    <ul>
    <% @origin.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
  	<% end %>
    </ul>
  </div>
  <% end %>
  
  <fieldset>
	  <%= f.input  :name, hint: 'Aim to capture all possible types in about 5-10 different categories.' %>
  </fieldset> 
 <br>
  <p>
    <%= f.submit %>
  </p>
<% end %>