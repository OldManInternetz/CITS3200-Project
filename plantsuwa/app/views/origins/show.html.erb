<% provide(:title, @origin.name) %>

<%= render partial: 'admin_pages/generic_admin_show', locals: {object: @origin, objects_path: origins_path, edit_object_path: edit_origin_path(@origin), object_path: origin_path(@origin), object_prevpage: 'Origins', object_name: 'origin', object_has_colour: false } %>

<p>Plants that are '<%= @origin.name %>':</p>
<%= render 'admin_pages/plants_table' %>