<%= simple_form_for(@notification, html: { class: 'form-horizontal' , :'accept-charset' => "UTF-8"}, defaults: { input_html: { class: 'form-control' } }) do |f| %>
  <% if @notification.errors.any? %>
  <div id="error_explanation">
    <br/>
    <h4><%= pluralize(@notification.errors.count, "error") %> prohibited this notification from being saved:</h4>
    <ul>
    <% @notification.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
  	<% end %>
    </ul>
  </div>
  <% end %>
  
  <fieldset>
	  <%= f.input  :name, hint: 'Aim to capture all possible types in about 5-10 different categories.', autofocus: true %>
  </fieldset> 
  <hr/>
  <%= f.submit class: 'btn btn-orange' %>

<% end %>