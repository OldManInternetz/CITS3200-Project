<% provide(:title, @type.name) %>

<%= render partial: 'admin_pages/generic_admin_show', locals: {object: @type, objects_path: types_path, edit_object_path: edit_type_path(@type), object_path: type_path(@type), object_prevpage: 'Types', object_name: 'type', object_has_colour: false } %>

<p>Plants that are '<%= @type.name.pluralize %>':</p>
<%= render 'admin_pages/plants_table' %>