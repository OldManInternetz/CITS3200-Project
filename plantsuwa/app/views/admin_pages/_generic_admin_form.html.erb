<% if object.new_record? %>
  <strong>Note that these changes will be applied to all plants with this climate.</strong>
<% end %>
<%= simple_form_for(object, html: { class: 'form-horizontal' , :'accept-charset' => "UTF-8"}, defaults: { input_html: { class: 'form-control' } }) do |f| %>
  <% if object.errors.any? %>
  <div id="error_explanation">
    <br/>
    <h4><%= pluralize(object.errors.count, "error") %> prohibited this <%= object_name %> from being saved:</h4>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
  	<% end %>
    </ul>
  </div>
  <% end %>
  <fieldset>
	  <%= f.input  :name, hint: 'Aim to capture all possible types in about 5-10 different categories.', autofocus: true %>
  </fieldset> 
  <hr/>
  <%= f.submit class: 'btn btn-orange' %>
<% end %>