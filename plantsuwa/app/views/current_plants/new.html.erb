<% provide(:title, 'New Plant') %>
<% provide(:prev_page, 'List') %>
<% provide(:prev_link, current_plants_path) %>

<%= render 'form' %>
 
<%= link_to 'Back', current_plants_path, class: 'btn-primary' %>
