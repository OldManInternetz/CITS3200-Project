<% provide(:title, 'Edit Plant') %>
<% provide(:prev_page, @current_plant.name) %>
<% provide(:prev_link, current_plant_path(:id => @current_plant.id)) %>

<%= render 'form' %>
 
<%= link_to 'Back', current_plants_path, class: 'btn-primary' %>
